name: notifications_test_app
description: A simple Flutter app for testing notifications
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # إشعارات
  flutter_local_notifications: ^17.0.0

  # timezone للجدولة
  timezone: ^0.9.2

  # أيقونات iOS (اختياري)
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true